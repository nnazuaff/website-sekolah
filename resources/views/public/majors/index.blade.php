@extends('layouts.public')
@section('title', 'Program Keahlian')
@section('content')
@include('layouts._public-page-header', ['title' => 'Program Keahlian', 'description' => 'Kenali program keahlian yang tersedia di sekolah.'])
<section class="site-section">@if($majors->isEmpty()) @include('layouts._public-empty', ['title' => 'Belum ada program keahlian']) @else<div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">@foreach($majors as $major)<article class="site-card"><div class="image-placeholder">@if($major->image)<img src="{{ asset('storage/'.$major->image) }}" alt="{{ $major->name }}" class="h-52 w-full object-cover">@else<span>{{ $major->short_name }}</span>@endif</div><div class="p-5"><p class="text-sm font-bold text-brand-700">{{ $major->short_name }}</p><h2 class="mt-2 text-xl font-bold text-brand-950">{{ $major->name }}</h2>@if($major->description)<p class="mt-3 text-sm leading-6 text-slate-600">{{ Str::limit($major->description, 140) }}</p>@endif<a href="{{ route('majors.show', $major->slug) }}" class="site-link mt-4 inline-block text-sm">Lihat detail</a></div></article>@endforeach</div>@endif</section>
@endsection